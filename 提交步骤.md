# 提交并发布到 GitHub

供开发者本地查看，不会出现在文档站菜单中。

改完文档后，按下面步骤提交并发布到线上站点。

## 1. 进入项目目录

```powershell
cd d:\AndroidCode\docs-site
```

## 2. 查看改动

```powershell
git status
git diff
```

确认要提交的文件没问题。

## 3. 加入暂存区

```powershell
git add .
```

出现 `LF will be replaced by CRLF` 可以忽略。

## 4. 提交

如果本机还没配过 Git 用户名，先在本仓库设一次（只需做一次）：

```powershell
git config user.name "laiming8787-oss"
git config user.email "laiming8787-oss@users.noreply.github.com"
```

然后提交：

```powershell
git commit -m "简要说明这次改了什么"
```

示例：

```powershell
git commit -m "Remove sidebar items after electronic scale development."
```

## 5. 推送到 GitHub

```powershell
git push
```

第一次若提示登录，按浏览器或 Token 完成即可。

## 6. 等自动发布

1. 打开：https://github.com/UrovoSdkCenter/docs-site/actions
2. 等 **Deploy Docs** 变绿
3. 刷新站点：https://urovosdkcenter.github.io/docs-site/

## 最短版

配好用户名之后，日常只需：

```powershell
cd d:\AndroidCode\docs-site
git add .
git commit -m "更新说明"
git push
```
