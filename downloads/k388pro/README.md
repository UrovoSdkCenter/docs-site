# K388Pro SDK downloads (v2.4.2)

Unified Local + BLE print SDK.
Page builders: `buildCpcl()` / `buildPageBytes()` (send via `printByText` / `printByData`).

| File | Description |
|------|-------------|
| `K388Pro_Printer_SDK.zip` | SDK v2.4.2 package (`SDK/` JAR, `DemoCode/`, `DemoAPK/`, `Docs/`) |
| `k388-printer-sdk-sdk-docs.zip` | Offline API docs package (zip; extract and open the HTML) |
